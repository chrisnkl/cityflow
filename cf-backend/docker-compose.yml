
services:

  database:
    image: postgres:15-alpine
    container_name: cf-backend-db
    environment:
      POSTGRES_DB: cf-db
      POSTGRES_USER: cf-user
      POSTGRES_PASSWORD: cf-password
    ports:
      - "5432:5432"
    volumes:
        - cf-database-data:/var/lib/postgresql/data

  kafka:
    image: apache/kafka:4.0.1
    container_name: cf-backend-kafka
    ports:
      - "9092:9092"

volumes:
    cf-database-data: